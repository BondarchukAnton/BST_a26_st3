# Инструкция по запуску автономной миссии «Дрон + Ровер»

## Быстрый старт

```bash
# 1. Активировать виртуальную среду (если используется)
source venv/bin/activate

# 2. Установить зависимости
pip install paramiko openai ultralytics

# 3. Запустить миссию
python3 main.py
```

## Структура проекта

```
BST_a26_st3/
├── main.py                      # Точка входа — оркестратор всей миссии
├── config/
│   └── settings.py              # Все настраиваемые параметры
├── src/
│   ├── logger.py                # Логирование (русский язык, формат Task.md)
│   ├── gemma_vlm.py             # VLM-анализ изображений (Gemma4-VLM)
│   ├── drone_mission.py         # Сборка и запуск бортового скрипта дрона
│   └── rover_mission.py         # Миссия ровера
├── modules/                     # Исходные отработанные модули (НЕ ИЗМЕНЯТЬ)
│   ├── yolo_search_vlm_best.py  # YOLO-поиск + VLM на дроне
│   ├── aruco_land.py            # Посадка дрона на ArUco-метку
│   ├── mission5_chushka_1.py    # Миссия ровера
│   └── gemma_vlm.py             # VLM-обёртка
├── examples/                    # Примеры (НЕ ИЗМЕНЯТЬ)
├── logs/                        # Логи миссии (создаётся автоматически)
└── best.pt                      # Обученная YOLO-модель
```

## Порядок выполнения миссии

```
ФАЗА 1: ДРОН — ПОИСК + ПОСАДКА
  ├── Взлёт с точки старта (A6)
  ├── Циклический облёт точек A1 и E2
  │     ├── YOLO-поиск объекта «Чебурашка» в реальном времени
  │     ├── При обнаружении: VLM-верификация + синий LED (5 сек)
  │     └── Сохранение фото находки и результата VLM
  ├── Полёт к позиции ровера (0.4, 2.01)
  └── Посадка на ArUco-метку 332 (на крыше ровера)

ФАЗА 2: ПАУЗА
  └── Ожидание 15 секунд после посадки

ФАЗА 3: РОВЕР — ДВИЖЕНИЕ К ЦЕЛИ + ПОИСК ФИНИША
  ├── Получение координат цели:
  │     • Находка у A1 (0.4, 4.425) → цель ровера (1, 1)
  │     • Находка у E2 (1.205, 1.205) → цель ровера (5, 2)
  ├── Движение к коробу с объектом
  ├── VLM-поиск финишной клетки (фото + анализ)
  └── Движение к финишной клетке
```

## Параметры командной строки

| Параметр | По умолчанию | Описание |
|---|---|---|
| `--drone-ip` | `192.168.1.37` | IP-адрес дрона |
| `--rover-ip` | `192.168.1.185` | IP-адрес ровера |
| `--altitude` | `2.0` | Рабочая высота дрона (метры) |

### Примеры запуска

```bash
# Стандартный запуск с параметрами по умолчанию
python3 main.py

# Указание IP-адресов
python3 main.py --drone-ip 192.168.1.100 --rover-ip 192.168.1.200

# Изменение высоты полёта
python3 main.py --altitude 2.5

# Все параметры вместе
python3 main.py --drone-ip 192.168.1.100 --rover-ip 192.168.1.200 --altitude 2.5
```

## Настройка через переменные окружения

Все параметры можно задать через переменные окружения (имеют приоритет над `config/settings.py`):

```bash
# Linux / macOS
export DRONE_IP="192.168.1.100"
export ROVER_IP="192.168.1.200"
export LOCAL_MODEL_PATH="/home/user/my_model.pt"
python3 main.py

# Одной строкой
DRONE_IP="192.168.1.100" ROVER_IP="192.168.1.200" python3 main.py
```

## Настраиваемые параметры в `config/settings.py`

### Точки поиска дрона

```python
WAYPOINT_A1 = (0.4, 4.425)     # Первая точка поиска (A1)
WAYPOINT_E2 = (1.205, 1.205)   # Вторая точка поиска (E2)
```

### Позиция ровера

```python
ROVER_POSITION = (0.4, 2.01)   # Куда летит дрон для посадки
```

### Соответствие точек → цель ровера

```python
ROVER_TARGETS = {
    WAYPOINT_A1: (1, 1),       # Находка у A1 → ровер едет в (1, 1)
    WAYPOINT_E2: (5, 2),       # Находка у E2 → ровер едет в (5, 2)
}
```

### Параметры полёта

| Параметр | Значение | Описание |
|---|---|---|
| `ALTITUDE` | `2.0` | Рабочая высота (м) |
| `SPEED` | `0.5` | Скорость полёта (м/с) |
| `SEARCH_DURATION` | `8.0` | Время поиска у каждой точки (сек) |
| `RANDOM_OFFSET_RANGE` | `0.3` | Случайное смещение при повторных проходах (м) |
| `FLY_TO_ROVER_SPEED` | `0.7` | Скорость полёта к роверу (м/с) |

### ArUco-посадка

| Параметр | Значение | Описание |
|---|---|---|
| `ROVER_ARUCO_ID` | `332` | ID метки на ровере |
| `SEARCH_ALTITUDE` | `1.8` | Высота поиска метки (м) |
| `MIN_LAND_ALTITUDE` | `0.4` | Минимальная высота перед посадкой (м) |
| `ARUCO_SEARCH_TIMEOUT` | `15.0` | Макс. время поиска метки (сек) |

### Временные задержки

| Параметр | Значение | Описание |
|---|---|---|
| `LANDING_DELAY` | `15.0` | Пауза после посадки перед стартом ровера (сек) |
| `LED_DURATION` | `5.0` | Длительность синей LED-индикации (сек) |
| `TAKEOFF_SETTLE_TIME` | `5.0` | Время стабилизации после взлёта (сек) |
| `FLY_SETTLE_TIME` | `4.0` | Время стабилизации после перелёта (сек) |

### YOLO

| Параметр | Значение | Описание |
|---|---|---|
| `YOLO_MODEL_NAME` | `yolo11n.pt` | Имя модели (если своя не загружена) |
| `YOLO_CLASS_NAME` | `None` | Имя класса (None — любой) |
| `YOLO_CONFIDENCE` | `0.5` | Порог уверенности детекции |
| `LOCAL_MODEL_PATH` | зависит от ОС | Путь к своей модели |

### VLM

| Параметр | Значение | Описание |
|---|---|---|
| `VLM_API_KEY` | `sk-...` | API-ключ |
| `VLM_API_BASE` | `https://ai.sverk.tech/v1` | Базовый URL API |
| `VLM_MODEL` | `gemma4-vlm` | Имя модели |

### Подключения

| Параметр | Значение | Описание |
|---|---|---|
| `DRONE_IP` | `192.168.1.37` | IP дрона (SSH) |
| `DRONE_USER` | `sverk` | Пользователь дрона |
| `DRONE_PASSWORD` | `sverk` | Пароль дрона |
| `ROVER_IP` | `192.168.1.185` | IP ровера (SSH + HTTP) |
| `ROVER_USER` | `pi` | Пользователь ровера |
| `ROVER_PASSWORD` | `raspberry` | Пароль ровера |

## Результаты работы

После завершения миссии в корне проекта появятся:

| Файл | Содержимое |
|---|---|
| `logs/mission_20260802_143025.log` | Полный лог миссии |
| `cheburashka_detected.jpg` | Фото находки с дрона |
| `vlm_result.json` | Результат VLM-анализа |
| `gemm_rover_photo.jpg` | Фото с камеры ровера (для VLM) |

## Формат лога

Логи пишутся на русском языке в формате, рекомендованном `Task.md`:

```
2026-08-02T10:15:30.123Z координатор МИССИЯ ЗАПУЩЕНА
2026-08-02T10:15:32.456Z координатор ФАЗА 1: ДРОН: ПОИСК ОБЪЕКТА + ПОСАДКА НА РОВЕР
2026-08-02T10:15:35.789Z дрон устойчивая_детекция id=1 доверие=0.94 кадров=1 точка=A1
2026-08-02T10:15:35.801Z дрон отправка_координат id=2 точка_цели=A1=(0.4, 4.425)
```

Формат строки: **`время агент событие id=N ключ=значение ...`**

Лог содержит метки для трёх рекордов соревнования (см. `Task.md`):
- **Рекорд 1** — самое быстрое целеуказание: `устойчивая_детекция`, `отправка_координат`, `приём_координат`, `команда_движения`
- **Рекорд 2** — плотный канал связи: `отправка`, `приём` (с отдельным счётчиком `id` сообщений)
- **Рекорд 3** — быстрый бортовой VLM: метки на дроне доступны через вывод скрипта (`[VLM] ответ за ... мс`)

## Устранение неполадок

### Дрон не взлетает
- Проверить IP-адрес: `ping 192.168.1.37`
- Проверить EKF: дождаться `GPS-to-ArUco handoff complete`
- Проверить заряд батареи: `ros2 topic echo /fmu/out/battery_status`

### Ровер не едет
- Проверить `stop_latched`: выполнить `clear`
- Проверить `nav_not_ready`: выполнить `initial-cell`
- Проверить статус: `python3 tools/rover_control_client.py --url http://IP:8767 state`

### VLM не работает
- Проверить API-ключ в `config/settings.py`
- Проверить сетевой доступ: `curl https://ai.sverk.tech/v1/models`
- VLM возвращает мусор: скрипт автоматически очищает markdown-обёртку

### SSH connection refused
- Дождаться загрузки дрона/ровера (1-2 минуты после включения)
- Проверить IP-адрес
- Проверить, что устройства в одной сети